version: 0.0
Resources:
  - myApp:
      Type: AWS::EKS::Application
      Properties:
        Name: brain-tasks-app
        ClusterName: <myappcluster>
        Namespace: default

        FileExistsBehavior: OVERWRITE
